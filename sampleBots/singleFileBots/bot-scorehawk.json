{
  "name": "ScoreHawk",
  "build": {
    "maxSpeedTier": 2,
    "turnRateTier": 4,
    "sightRangeTier": 4,
    "sightFovTier": 2,
    "shotPowerTier": 4,
    "shotSpeedTier": 2,
    "maxHealthTier": 3,
    "memoryTier": 0,
    "wallBehavior": "stop"
  },
  "behavior": {
    "tick": "function tick(api) { let scanResult = api.scan(60); if (scanResult.found === false) { api.turn(8); api.advance(0.6); } else { api.turn(scanResult.angle); api.advance(0.7); let aligned = api.aligned(scanResult.angle, 6); if (aligned) { api.fire(); } } }"
  }
}
