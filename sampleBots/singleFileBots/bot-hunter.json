{
  "name": "Sharpshade",
  "build": {
    "maxSpeedTier": 3,
    "turnRateTier": 3,
    "sightRangeTier": 3,
    "sightFovTier": 2,
    "shotPowerTier": 4,
    "shotSpeedTier": 3,
    "maxHealthTier": 3,
    "memoryTier": 0,
    "wallBehavior": "stop"
  },
  "behavior": {
    "tick": "function tick(api) { let scanResult = api.scan(60); if (scanResult.found === true) { api.turn(scanResult.angle); api.advance(0.8); let aligned = api.aligned(scanResult.angle, 4); if (aligned) { api.fire(); } } else { api.turn(8); api.advance(0.9); } }"
  }
}
