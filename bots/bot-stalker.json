{
  "name": "StalkerX",
  "build": {
    "maxSpeedTier": 3,
    "turnRateTier": 3,
    "sightRangeTier": 3,
    "sightFovTier": 2,
    "shotPowerTier": 4,
    "shotSpeedTier": 3,
    "maxHealthTier": 2,
    "memoryTier": 0,
    "wallBehavior": "bounce"
  },
  "behavior": {
    "tick": "function tick(api) { let scanResult = api.scan(60); if (scanResult.found === true) { api.turn(scanResult.angle); api.advance(1.0); let aligned = api.aligned(scanResult.angle, 6); if (aligned) { api.fire(); } } else { api.turn(10); api.advance(0.9); } }"
  }
}
